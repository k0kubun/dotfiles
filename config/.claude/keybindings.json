{
  "$schema": "https://www.schemastore.org/claude-code-keybindings.json",
  "bindings": [
    {
      "context": "Task",
      "bindings": {
        "ctrl+b": null
      }
    },
    {
      "context": "Chat",
      "bindings": {
        "ctrl+b": null
      }
    }
  ]
}
